const valores = [2, 5, 6, 7];

// recorre el array y muestra el valor del indice del elemento
/*for(let i=0; i < valores.length; i++) {
    console.info(valores[i]);
}*/

// recorre el array y mustra el valor del indice del elemento

/*for(let val of valores) {
    console.info(val);
}*/

// recorre el array y muestra el indice y su valor
/*for(let i in valores) {
    console.info(i + " - " + valores[i]);
}*/


let i = 0;
while(i<5) {
    console.info(i);
    i++; // i = i + 1
}